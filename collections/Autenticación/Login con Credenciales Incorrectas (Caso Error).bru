meta {
  name: Login con Credenciales Incorrectas (Caso Error)
  type: http
  seq: 3
}

post {
  url: {{backend_url}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "username": "usuario_prueba",
      "password": "contraseña_incorrecta"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
